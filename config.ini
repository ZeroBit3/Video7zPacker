[Settings]
# 默认加密密码
password = MySuperSecretPassword
# 默认输出目录
output_dir = D:\Video_Backup

# 进阶静默控制 (可选)
# 目标目录有同名文件时，是否自动覆盖 (true/false)
auto_overwrite = false
# 无法识别集数时，是否自动使用原文件名打包 (y/n/留空)，留空则回退到手动 input 输入
auto_name_fallback = y